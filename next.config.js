/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["tugasguru.com", "beta.tugasguru.com", "source.unsplash.com"]
  }
}

module.exports = nextConfig
